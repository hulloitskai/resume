---

apiVersion: v1
kind: Service

metadata:
  name: resume
  namespace: default

spec:
  type: ClusterIP
  selector:
    app: resume
    tier: frontend
    track: stable
  ports:
    - name: http
      protocol: TCP
      port: 80
